Для нормального функционирования обязательно скачать шрифты Arial
на linux
sudo apt install ttf-mscorefonts-installer
sudo fc-cache -f

jdbc:postgresql://${JDBC_DATABASE_URL}
для запуска с компа на idea